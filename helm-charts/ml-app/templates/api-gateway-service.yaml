apiVersion: v1
kind: Service
metadata:
  name: {{ include "ml-app.fullname" . }}-{{ .Values.apiGateway.name }}
  labels:
    {{- include "ml-app.labels" . | nindent 4 }}
    app.kubernetes.io/component: {{ .Values.apiGateway.name }}
spec:
  type: {{ .Values.apiGateway.serviceType }}
  ports:
    - port: {{ .Values.apiGateway.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ml-app.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: {{ .Values.apiGateway.name }}
