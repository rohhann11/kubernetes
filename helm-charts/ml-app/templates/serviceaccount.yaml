apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ml-app.serviceAccountName" . }}
  labels:
    {{- include "ml-app.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
