apiVersion: v1
kind: Service
metadata:
  name: {{ include "ml-app.fullname" . }}-{{ .Values.modelTraining.name }}
  labels:
    {{- include "ml-app.labels" . | nindent 4 }}
    app.kubernetes.io/component: {{ .Values.modelTraining.name }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.modelTraining.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ml-app.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: {{ .Values.modelTraining.name }}
